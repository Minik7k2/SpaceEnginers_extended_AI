# brain (C++)
brain/configs/*.local.toml
# Configi doraźnych harnessów (np. rules.test.toml pod ręczny test promptu na Bieliku):
# wskazują ścieżki z KONKRETNEJ maszyny i katalogi scratchpad, więc nie mają czego szukać
# w repo. Reguła wyżej łapała tylko *.local.toml, przez co `git add -A` wciągał je razem
# z cudzymi ścieżkami (2026-08-04).
brain/configs/rules.test.toml
brain/build/
brain/cmake-build-*/
brain/models/
brain/state/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# mostek JSONL — dane runtime, nie kod
storage/
*.jsonl
# WYJĄTEK: scenariusze testowe to KOD, nie dane runtime. Bez tej linii `*.jsonl` połykało
# je po cichu — pliki nigdy nie trafiły do repo, `file(GLOB)` w CMake nie znajdował ani
# jednego, ctest pokazywał 4 testy zamiast 7 i CI świeciło na zielono, nie uruchamiając
# NICZEGO z tej warstwy. Guard w brain/tests/CMakeLists.txt pilnuje, żeby się nie powtórzyło.
!brain/tests/scenariusze/*.jsonl

# mod (C#)
mod/bin/
mod/obj/
# binarny cache definicji generowany przez SE obok .sbc (odtwarzalny)
*.sbcB5

# ogólne
*.log
.vs/
.vscode/
.idea/
